# Pac-Man contribution graph setup

1. Open the repository's **Actions** tab and run **Generate Pac-Man contribution graph** once.
2. Wait for the workflow to create or refresh the `output` branch.
3. If the workflow cannot publish, open **Settings → Actions → General → Workflow permissions**, choose **Read and write permissions**, and run it again.

The workflow refreshes the Pac-Man contribution graph every day.
